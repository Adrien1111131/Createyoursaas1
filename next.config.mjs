/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuration optimisée pour la production
  images: {
    domains: [], // Ajoutez ici les domaines externes pour les images si nécessaire
  }
}

export default nextConfig
